Sources for the plugin `plugin.cafebazaar.iap`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.cafebazaar.iap" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
